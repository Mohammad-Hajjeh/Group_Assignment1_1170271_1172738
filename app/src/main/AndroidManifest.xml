<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.bzu.group_assignment1_1170271_1172738">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.DeleteCourse"></activity>
        <activity android:name=".Activities.SignupAdmin" />
        <activity android:name=".Activities.Menu" />
        <activity android:name=".Activities.SearchCourse" />
        <activity android:name=".Activities.UpdateCourse" />
        <activity android:name=".Activities.AddCourse" />
        <activity android:name=".Activities.ShowCourses" />
        <activity android:name=".Activities.LoginAdmin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>